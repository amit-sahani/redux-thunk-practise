import React from 'react';
import {connect} from 'react-redux';

const PostList = () =>{

    return (<div>
                Post
            </div>
    );
}

export default connect(null, {})(PostList);